name: CI/CD Pipeline

permissions:
  contents: write
  pull-requests: read

on:
  push:
    branches:
      - '**'
  pull_request:

jobs:
  lint:
    name: Linter
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.25.1"
          cache: true

      - name: Run linter
        run: make lint

  test:
    name: Unit Tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.25.1"
          cache: true

      - name: Run tests
        run: make test

  migrate:
    name: Database Migrations
    # запускаем только при пуше в stage/main
    if: github.event_name == 'push' && (github.ref == 'refs/heads/stage' || github.ref == 'refs/heads/main')
    needs: [ lint, test ]
    runs-on: ubuntu-latest
    environment: ${{ github.ref_name == 'main' && 'prod' || 'stage' }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - name: Check for migration changes
        id: changed-files
        uses: tj-actions/changed-files@v44
        with:
          files: migrations/*.sql

      - name: Apply migrations
        # Запускаем если изменились файлы миграций ИЛИ если есть флаг в коммите
        if: steps.changed-files.outputs.any_changed == 'true' || contains(github.event.head_commit.message, '[force-migrate]')
        env:
          POSTGRES_DSN: ${{ secrets.POSTGRES_DSN }}
        run: make migrate-up

  release:
    name: Create Release Tag
    # только push в main
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    needs: [ migrate ]
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.25.1"
          cache: true

      - name: Get latest tag
        id: get_tag
        run: |
          git fetch --tags
          latest_tag=$(git tag --sort=-v:refname | head -n 1)
          if [[ -z "$latest_tag" ]]; then latest_tag="v0.0.0"; fi
          
          # Очистка тега от префикса v для инкремента
          tag_num=${latest_tag#v}
          IFS='.' read -r major minor patch <<< "$tag_num"
          new_tag="v$major.$minor.$((patch + 1))"
          
          echo "tag=$new_tag" >> $GITHUB_ENV
          echo "New tag: $new_tag"

      - name: Create and push tag
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git tag "${{ env.tag }}"
          git push origin "${{ env.tag }}"
